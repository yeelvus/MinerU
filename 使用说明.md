# MinerU 本地部署与使用说明

部署位置：`/Users/wyattsun/Projects/01_文档解析与转换/MinerU`  
Conda 环境：`mineru`（Python 3.12）  
已安装版本：MinerU 3.4.4

本机为 **Apple M1 Pro**，推荐使用 **pipeline** 后端（稳定、省内存）。需要更高精度时可改用 `hybrid-engine`。

---

## 一、启动图形界面（推荐）

在终端执行：

```bash
cd /Users/wyattsun/Projects/01_文档解析与转换/MinerU
./start_gui.sh
```

或：

```bash
conda activate mineru
cd /Users/wyattsun/Projects/01_文档解析与转换/MinerU
python mineru_gui.py
```

### 界面操作

1. 点击 **「选择文件…」** 选单个 PDF，或 **「选择文件夹…」** 批量处理目录内文档  
2. 确认 **输出目录**（默认 `MinerU/output`）  
3. 参数建议：
   - **后端**：`pipeline`（Mac 推荐）
   - **方法**：`auto`
   - **语言**：`ch`（中文）
   - **模型源**：`modelscope`（国内网络）
4. 点击 **「开始处理」**，等待完成  
5. 点击 **「打开输出目录」** 查看结果  

支持格式：`PDF`、图片、`DOCX`、`PPTX`、`XLSX`

---

## 二、命令行使用

```bash
conda activate mineru
export MINERU_MODEL_SOURCE=modelscope   # 国内推荐

# 处理单个 PDF
mineru -p /path/to/file.pdf -o /path/to/output -b pipeline

# 处理整个文件夹
mineru -p /path/to/pdf_folder -o /path/to/output -b pipeline

# 更高精度（占用更多内存）
mineru -p /path/to/file.pdf -o /path/to/output -b hybrid-engine --effort medium
```

---

## 三、可选：WebUI / API

```bash
conda activate mineru
export MINERU_MODEL_SOURCE=modelscope

# Gradio 网页界面
mineru-gradio --server-name 127.0.0.1 --server-port 7860
# 浏览器打开 http://127.0.0.1:7860

# FastAPI 服务
mineru-api --host 127.0.0.1 --port 8000
# 文档 http://127.0.0.1:8000/docs
```

---

## 四、输出说明

解析结果默认在输出目录下，按文件名分文件夹，常见内容包括：

| 文件 | 说明 |
|------|------|
| `*.md` | Markdown 正文 |
| `*_content_list.json` | 结构化内容列表 |
| `images/` | 提取的图片 |
| 中间 JSON | 布局与解析细节 |

---

## 五、注意事项

1. **首次运行**会下载模型，体积较大，请保持网络畅通；国内请用 `MINERU_MODEL_SOURCE=modelscope`。  
2. 系统自带 Python 3.14 **不兼容**，请务必 `conda activate mineru`。  
3. 内存建议 16GB+；长文档若 OOM，可改用 `pipeline` 或减少并发。  
4. 停止处理：GUI 点「停止」，或终端 `Ctrl+C`。  

---

## 六、环境维护

```bash
# 激活环境
conda activate mineru

# 升级 MinerU
uv pip install -U "mineru[all]"

# 查看版本
mineru --version
```
